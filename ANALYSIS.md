# Titan2Elite Floating Bar — 바이러스 검사 & Android 16 개조 리포트

원본 APK: `com.tools.lgv30.floatingbar` (LG V30 Floating Bar), 버전 **1.1.0 (10)**
결과물: **`Titan2Elite Floating Bar.apk`** (targetSdk 36 / Android 16 대응, 재서명 완료)

---

## 1. 요약

| 항목 | 내용 |
|------|------|
| 악성코드(바이러스) 검사 | **깨끗함 (악성 지표 없음)** |
| Android 16 대응 | targetSdk 26 → **36** 상향 + 크래시 유발 지점 패치 |
| 서명 | zipalign + **v1 + v2 + v3** 스킴으로 재서명 (검증 통과) |
| 최소 지원 | minSdk 21 (Android 5.0) 유지 → Android 5.0 ~ 16 설치 가능 |

> 원본을 수정했기 때문에 원 개발자 키로 서명할 수 없어 **새 키로 재서명**했습니다.
> 따라서 기존에 설치된 원본 위에 업데이트되지 않고 **새로 설치**됩니다(서명 불일치).

---

## 2. 바이러스 / 악성코드 검사 결과 — **깨끗함**

APK를 디컴파일(jadx)하여 전체 코드/리소스/서명을 정적 분석했습니다. 알려진 악성 행위 지표를 전수 조사한 결과, **악성코드는 발견되지 않았습니다.**

| 점검 항목 | 결과 |
|-----------|------|
| SMS/유료문자 발송 (`SmsManager`) | 없음 |
| 동적 코드 로딩 (`DexClassLoader`, `System.load`) | 없음 |
| 네이티브 라이브러리(.so) | 없음 |
| 쉘 실행 (`Runtime.exec`, `su`) | 없음 |
| 광고/추적 SDK (AdMob, Firebase, Facebook, Flurry 등) | 없음 |
| 기기 식별자 수집 (IMEI, ANDROID_ID, 계정) | 없음 |
| 외부 서버(C2) 통신 | 없음 (아래 참고) |
| WebView 원격코드/JS 인터페이스 | 없음 (JS 비활성화, 로컬 FAQ만 로드) |

**네트워크 통신은 날씨 기능 한 곳뿐입니다.**
- `https://api.openweathermap.org` (날씨 조회 — OpenWeatherMap 공개 API)
- 위치 정보는 **날씨 조회 URL 구성에만** 사용되며 개발자 서버로 전송되지 않습니다.
- 연락처/전화 권한은 플로팅바의 **즐겨찾기 빠른전화(로컬)** 기능에만 쓰이며 네트워크로 나가지 않습니다.

**강력한 권한들은 모두 실제 기능과 1:1로 대응**됩니다(정상 유틸리티):
- `SYSTEM_ALERT_WINDOW` → 화면 위 플로팅바 오버레이
- 접근성 서비스 → 뒤로/홈/최근앱 동작
- 알림 리스너 → 플로팅바에서 음악/알림 제어
- 기기 관리자 → **화면 잠금 버튼**용 `lockNow()` 만 사용 (초기화/와이프 없음)
- `WRITE_SETTINGS` → 밝기/회전 토글, 위치 → 날씨, 카메라 → 손전등, WiFi/BT/오디오 → 빠른 토글

**서명 정보**
- 원본 개발자: `CN=Bui Minh Viet, O=Megavietbm, Ha noi, VN` (자체 서명, 정상)
  - SHA256 `09:9E:6D:C2:4A:75:86:6C:...:B4:32:12`

> 참고: 정적 분석 기준입니다. 접근성·알림리스너·기기관리자 등 민감 권한을 쓰는 앱이므로,
> 사용 시 필요한 기능의 권한만 허용하시길 권장합니다. (기능상 필요 범위 내이며 악용 코드는 없음)

---

## 3. Android 16(API 36) 개조 내역

targetSdk를 26 → **36**으로 올리면 Android 12~16에서 강제되는 규칙 때문에 **설치 거부/실행 크래시**가 발생합니다. 이를 모두 해결했습니다.

### 3-1. 매니페스트(AndroidManifest.xml)
- **`android:exported` 명시** — 모든 컴포넌트(액티비티/서비스/리시버) 19개에 지정
  (Android 12+ 필수. 없으면 **설치 자체가 거부**됨)
  - 런처/시스템 바인딩 컴포넌트 6개 = `true` (Splash, 접근성/타일/알림리스너 서비스, 부팅/기기관리자 리시버)
  - 내부 전용 컴포넌트 = `false`
- **`POST_NOTIFICATIONS` 권한 추가** (Android 13+ 알림 표시용)
- **패키지 가시성(Android 11+)**: `QUERY_ALL_PACKAGES` 대신 **범위 지정 `<queries>`**(MAIN/LAUNCHER)로 처리 — 앱 바로가기 목록/실행 기능 유지하면서 Play 프로텍트가 민감하게 보는 광범위 권한을 제거
- **`usesCleartextTraffic="true"`** 추가 (평문 트래픽 차단 정책 대비 안전장치)

### 3-1b. 리소스(표시 이름 · 아이콘)
- **앱 표시 이름 → "타이탄2엘리트 플로팅바"** (`<application android:label>`을 리소스 참조 대신 **리터럴 문자열**로 지정 → "다른 앱 위에 표시" 등 특수권한 설정 화면에서 패키지명(com.tools.lgv30.floatingbar)이 아닌 앱 이름이 안정적으로 표시됨). 기본/타 언어 라벨은 "Titan2Elite Floating Bar" 유지
- **앱 아이콘 `ic_launcher` → TITAN 2 ELITE 이미지**로 교체 (mdpi~xxxhdpi 5종 밀도, **흰 배경 제거해 투명 처리**, 글로우 가장자리 페더링)

### 3-2. 코드(smali) 패치 — 실행 크래시 방지
- **`PendingIntent` 불변 플래그** — 스크린샷 기능의 `PendingIntent`에 `FLAG_IMMUTABLE` 지정
  (Android 12+에서 플래그 누락 시 `IllegalArgumentException` 크래시)
- **`registerReceiver` 익스포트 플래그** — 브로드캐스트 리시버 등록 11곳을
  호환 헬퍼(`Cc.reg`/`Cc.regExp`)로 우회 처리
  (Android 14+에서 플래그 누락 시 `SecurityException` 크래시)
  - 내부/시스템 방송 9곳 → `RECEIVER_NOT_EXPORTED`
  - 인앱결제 방송 2곳 → `RECEIVER_EXPORTED`
  - 헬퍼는 `SDK_INT >= 33`에서만 새 API를 호출하므로 구버전(minSdk 21)도 안전
- **날씨 API `http` → `https`** 전환 (평문 통신 제거, 보안 강화)

### 3-3. 이미 호환되어 손대지 않은 부분
- 플로팅바 **오버레이 타입**은 이미 `SDK_INT>=26`에서 `TYPE_APPLICATION_OVERLAY`(2038) 사용 → 정상
- 정확 알람(SCHEDULE_EXACT_ALARM), 포그라운드 서비스 타입 → **미사용**이라 조치 불필요

### 3-4. 알려진 제한사항 (경미, 핵심 기능 아님)
- 스크린샷 캡처 후 뜨는 알림은 **알림 채널 미설정**이라 Android 8+에서 표시되지 않을 수 있음
  (앱은 정상 동작, 알림만 안 보임). 캡처 저장 자체는 동작.
- 인앱결제는 구형 방식(IInAppBillingService)이라 최신 Google Play 환경에서 동작이 제한될 수 있음(무료 기능엔 영향 없음).
- 일부 하드웨어 토글(WiFi 핫스팟 등)은 최신 안드로이드의 숨김 API 제한으로 제조사에 따라 동작하지 않을 수 있음(원본부터의 제약, 악성 아님).

---

## 4. 설치 방법 & 주의사항

1. 원본이 설치되어 있다면 **먼저 삭제**하세요(서명이 달라 덮어쓰기 불가).
2. `Titan2Elite Floating Bar.apk`를 기기로 옮겨 설치(출처를 알 수 없는 앱 허용 필요).
3. 첫 실행 시 앱이 요청하는 특수 권한(오버레이 표시, 설정 변경, 접근성, 알림 접근, 기기 관리자)을
   기능에 맞게 허용하세요. Android 13+에서는 **알림 권한**도 별도로 허용해야 알림이 보입니다.

---

## 4-b. Play 프로텍트(Play Protect) 안내

Play 프로텍트는 **APK가 끄거나 통과시킬 수 있는 것이 아니라, 기기(구글)에서 도는 검사 서비스**입니다. 사이드로딩(스토어 밖 설치) 앱은 구글이 처음 보는 앱이라 "확인되지 않은 앱" 경고가 뜰 수 있습니다. 이 앱은 접근성·기기관리자·오버레이 등 강력한 권한을 쓰기 때문에 경고 대상이 되기 쉽습니다(악성은 아님, 리포트 2장 참고).

**이번 빌드에서 줄인 것**
- Play 프로텍트가 특히 민감하게 보는 **`QUERY_ALL_PACKAGES` 권한을 제거**하고 범위 지정 `<queries>`로 대체 → 유해성 신호를 낮춤.
- 정식 릴리스 키로 v1+v2+v3 서명(디버그 키 아님).

**그래도 경고가 뜰 때 바로 설치하는 방법 (기기에서)**
1. 설치 팝업에서 **"자세히" → "무시하고 설치"(또는 "그래도 설치")** 를 누릅니다.
2. 계속 막히면 Play 프로텍트를 잠시 끄고 설치:
   Play 스토어 → 우측 상단 프로필 → **Play 프로텍트** → 톱니(설정) → **"Play 프로텍트로 앱 스캔" 끄기** → 설치 후 다시 켜기.

> 참고: 접근성·기기관리자 기능(플로팅바의 뒤로/홈/최근앱, 화면잠금 버튼)을 안 쓰신다면, 해당 서비스/권한을 빼서 유해성 신호를 더 낮춘 빌드를 만들 수 있습니다. 원하시면 말씀해 주세요.

---

## 5. 서명 키 정보 (업데이트용)

동일 서명으로 향후 재빌드/업데이트하려면 함께 제공된 키스토어를 사용하세요.

- 파일: `titan2elite.jks`
- alias: `titan2elite`
- 스토어/키 비밀번호: `titan2elite`
- 새 서명 인증서 SHA256: `a2:56:0a:e0:96:c8:3e:c7:...:d6:36:dc` (RSA 2048, 2054년까지 유효)

> 키스토어에는 개인키가 들어 있습니다. 유출되면 이 앱과 동일 서명의 앱을 누구나 만들 수 있으니 보관에 유의하세요.

---

## 6. 사용한 도구
- apktool 2.11.1 (디코드/리빌드)
- jadx 1.5.2 (디컴파일/분석)
- uber-apk-signer 1.3.0 (zipalign + v1/v2/v3 서명)
- keytool (JDK 21), pyaxmlparser (매니페스트 독립 검증)
